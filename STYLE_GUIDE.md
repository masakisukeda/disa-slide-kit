# DiSA 提案スライド作成ガイド

このガイドは、提案用スライド（HTML/CSS形式）の作成ルールとデザイン基準をまとめたものです。

ベースとなるテンプレートファイルとして `template.html` を用意しています。

---

## 0. AIツールでスライドを作成・編集する際の必須手順

### 作成時の判断順

レイアウトの型を先に選ばず、以下の順で判断する。デザインの細部はこのガイドに従って固定しつつ、構図は内容に合わせて選ぶ。

1. **先に資料の役割を固定する**: 体裁を作り始める前に、①読み手は誰か ②この資料の目的 ③読み手に判断・確認してほしいこと ④今回は載せない範囲、の4点を3〜5行で書き出す。ここが曖昧なまま作ると、情報はきれいに並ぶが「で、何を決めればいいのか」が伝わらない資料になる。
2. **内容から構図を決める**: 先に各スライドの結論と、情報どうしの関係（比較・時系列・因果・並列・選択肢など）を整理する。最初に枚数・カラム数・既存パターンを決めない。関係が決まってから、それに合うパターンを選ぶ。
3. **タイトルを結論の文にする**: 各スライドのタイトル（`.slide-title`）は「何の話か」ではなく「このページで言いたいこと」を文で書く（NG: `順位と引用率の関係` → OK: `順位が下がるほど、AIに拾われなくなる`）。タイトル直下の `.slide-subtitle` は、その結論の根拠や範囲（何と何を比べたか、いつのデータか）を添える場所にする。本文や図表は、その結論を支えるために配置する。タイトルだけを上から通し読みして話が通るかが判定基準（4章）。
4. **強調には予算がある**: 1枚で主役にするメッセージは原則1つとする。アクセント色・太字・大きな数値は同じ主役を支える箇所に絞り、複数の要素を同時に目立たせない。
5. **カードの本文は2行まで**: 3行目が要るなら、その内容は別の1枚に逃がす。1枚の本文が500字を超えたら、それは「見る資料」ではなく「読む資料」になっている。数字は文の中に埋めず（NG: `紹介約1,000件 → 応募約40件（4%）`）、主役の数字として大きく出し、説明は補足の段に落とす（9章②のサイズ表）。5枚で足りないなら9枚にする。枚数を増やすことは悪くない。

   **Why:** 同じテンプレートで作っても、1枚に詰める量で読みやすさが決まる。2026年9月、外部講師のウェビナー資料（51枚・1枚に主張1つ）と、自社の面談まとめ資料（5枚・1枚に480〜520字）を並べて比較したところ、差はデザインではなく密度だった。

新規スライドの作成やレイアウト追加を指示された場合、**着手前に必ず以下のファイルを読むこと**。

1. 上の判断順で内容と構図を決めた後、`patterns.html` を開き、その関係に合うパターン（比較テーブル、Before/Afterフロー、As-Is/To-Be/Action、4象限マトリクス、ロードマップ、タイムライン、目次、中表紙、数値ハイライト、人物紹介など）が既にあるか確認する。
2. 該当パターンがあれば、そのスライドHTMLと対応するCSSブロックを**そのままコピー**して案件の `index.html` に貼り付け、テキストや件数だけを差し替える。パターンは使うこと自体が目的ではないため、内容上の必然がない同じ型の反復は避ける。
3. **新しいCSSクラスやレイアウトを自己判断で作らない。** `patterns.html` に無い構成が必要な場合のみ、既存の変数（`--blue` 等）とコンポーネント設計思想（カード・グリッド・アクセント1色ルール）に合わせて新規実装し、採用が決まったら `patterns.html` に1パターンとして追加する（README.md の「新しい知見を残すとき」参照）。
4. 迷ったら `README.md` の「まず見るもの」の順（`STYLE_GUIDE.md` → `template.html` → `patterns.html`）に戻って確認する。自分の記憶や一般的なスライドデザインの知識で構成・CSSを作らない。
5. **「ご清聴ありがとうございました」等の定型的な結び・クロージングスライドは追加しない。** 意思決定の依頼や連絡先の明示など、内容として必要な場合のみ最終スライドに含める（詳細は9章「標準ストーリー構成」参照）。
6. **同じ `index.html` を複数のAIツールで同時に編集しない。** Google Drive等の同期フォルダ上のファイルに対し、2つのツールがほぼ同時に保存すると、書き込みが競合してHTMLタグが千切れる・別スライドの中身が混ざるといった構造崩れが起きる（実例あり）。編集は1ツールずつ、片方の保存が終わってから次のツールに引き継ぐ。
7. **AIツールでの編集直後は、文字化けが混入していないか確認する。** ツールによっては保存時にUTF-8とShift-JIS（CP932）を取り違えて日本語テキストが文字化けすることがある（例:「一般社団法人」が「荳闊ｬ遉ｾ蝗｣豕穂ｺｺ」のような文字列に化ける）。編集後は該当ファイルをテキストエディタ等で開き、日本語のフッターやタイトル周辺に不自然な文字列が無いか目視確認する。ターミナルが使える場合は以下のコマンドで機械的にもチェックできる。
   ```bash
   python3 -c "open('index.html', encoding='utf-8').read()" && echo "OK: 有効なUTF-8です"
   ```
   このコマンドがエラーを出す場合、ファイルに不正なバイト列が混入している。直前の正常なバージョンに戻すか、該当箇所を手動で修正すること。
8. **編集前にバックアップファイル（`.bak` 等）を作らない。** `patterns.html` のような大きな正本ファイルを触るときも、コピーを別名で残さずそのまま編集する。リポジトリはgit管理下なので履歴で戻せる。バックアップを別ファイルで持つと二重管理になり、どれが正本かわからなくなる（このガイドが「正本を1つに絞る」と言っているのと同じ理由）。安全確保は**編集後の検証**で行う。版を残す意図があるときだけ `old/` フォルダへ退避し、事前に確認を取ること。
9. **編集後は、枚数まわりの整合を機械的に確認する。** 7章のUTF-8チェックに加えて、以下が揃っているかを見る。ズレていればその場で直す（4章「スライド枚数を変更した場合の同期」も参照）。
   - `<section class="slide">` の数と、各スライドの `id="slide-N"` が1から連番であること（重複がないこと）
   - 各スライドのページ番号 `N / 総数` の N が連番で、総数が実際の枚数と一致していること
   - ナビゲーションの `<option>` の件数・value が、スライドのidと1対1で対応していること
   - 各 `<section>` 内で `<div>` の開閉数が一致していること（分割・挿入の直後はタグが千切れやすい）

---

## 1. 設計の基本方針
- **1ページ = 1つの `<section class="slide">`**: 各スライドはセクション単位で管理します。
- **基準解像度**: 横1280px × 縦720px（16:9）を基準にレイアウトを組みます。
- **標準ベース（ベンチマーク）**: 新しいスライドを作るときは、自社で「これが基準」と決めた実物を1本決め、構成・情報整理・見た目のまとまりをそこに揃えます。基準を決めずに1枚ずつ作ると、資料ごとに体裁がばらつきます。
- **デザイン変数**: 色やフォントサイズなどの共通スタイルは `:root` に定義されたCSS変数で制御します。
  - **既定パレットは AM95 OG Neon**（クールグレーのランプ ＋ ネオンイエロー1色）。`template.html` / `patterns.html` の `:root` を参照。
  - **アクセントの差し替えは表紙の `data-accent` 1箇所だけ。** 1枚目の `<section>` に `data-accent="#0b2e4f"` のようにカラーコードを1色書くと、末尾のスクリプトが派生色を計算して `:root` を上書きします。CSSは触りません。属性を書かなければ既定のネオンイエローのままです。それ以外の箇所に色をベタ書きしないこと。
    - **色決めは表紙のピッカーで。** 表紙の右下に制作用のカラーピッカーが出ます。色を選ぶかカラーコードを直接打つと、デッキ全体に即反映されて見比べられます。決まったら、そこに出ているコードを表紙の `<section>` の `data-accent` に書いてください（**ピッカーでの変更はファイルには保存されません**。書いて初めて残ります）。
    - **ピッカーは納品前に削除する。** 印刷・PDF書き出しでは自動的に消えますが、HTMLをそのまま渡す場合は残ります。`.accent-picker` のCSSブロック・表紙HTMLの `<div class="accent-picker">` ブロック・ピッカー用の `<script>` の3つが「制作用」とコメントで囲ってあるので、まとめて削除します。
    - 自動生成されるのは4つ。`--accent-deep`（ライト面の線・✓記号。白地で3:1を満たすまで暗くする）／`--accent-on-dark`（ダーク面の文字・バー。黒地で4.5:1を満たすまで明るくする）／`--on-accent`（アクセント塗りの上に乗る文字色）／`--accent-tint`（8%だけ白に混ぜた面）。
    - **この仕組みがあるので、濃紺や赤のような暗いコーポレートカラーを指定してもダーク面が潰れません。** 逆に言うと、新しくCSSを書くときは用途に応じて正しいトークンを選ぶこと。ダーク面に置く文字・バーに `--accent` を直接使うと、暗い色を指定されたときに読めなくなります。
  - グレーは `--ink-900`〜`--ink-100` のランプから取ります。面・区切り・本文テキストはすべてここから。個別に `#333` のような値を書かない。
  - 旧 `--blue` / `--brand-volt` / `--accent-gold` は既存案件との互換のためエイリアスとして残していますが、**新規では使いません**。
- **アクセントカラー**: スライドで使用するアクセントカラーは、提案先クライアントのコーポレートカラーに合わせて調整します。**1つのデッキにつきアクセントカラーは1色のみ**とし、複数の色（例: ゴールド系とローズ系を両方使う等）を混在させません。ダーク面／ライト面での明度違いや、同系色のsoft/deepといったトーン展開は1色の範囲内として問題ありません。
- **色の役割を増やさない**: 優先度、担当者名、ツール名などの補助情報に別色を割り当てません。アクセントは見出し、罫線、重要な数値など、情報階層を作る箇所に限定します。
- **使用フォント**: 欧文は `Inter`、和文は `Noto Sans JP` を使用します。他のフォント（Outfitなど）は原則使用しません。
- **アイコン**: 図形や絵文字で意味を補う必要がある箇所には、[Google Material Symbols](https://fonts.google.com/icons) を使用します。アイコンは見出し・数値・ステップなどの内容を補助する目的に限り、装飾のために多用しません。同一デッキ内では `Outlined` を基本スタイルとして統一し、アクセントカラーは文字・罫線と同じクライアントカラー1色のルールに従います。
  - HTMLの `<head>` で、必要なアイコンだけを指定したGoogle Fontsのstylesheetを読み込みます。例: `<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0&display=block">`
  - 本文では `<span class="material-symbols-outlined" aria-hidden="true">insights</span>` のように、[Material Symbolsの名称](https://fonts.google.com/icons)をテキストとして指定します。アイコン単体で意味を伝える場合は `aria-hidden` を外し、代わりに `aria-label` を付けます。
  - 原則 `font-size: 20px` を基準とし、見出し横などはテキストの行高に合わせて調整します。絵文字・Unicode記号・個別のSVGアイコンを混在させず、ブランドロゴや図解に固有のアイコンが必要な場合のみ例外とします。


---

## 2. HTMLの基本構造
スライド全体は `#presentation-viewport` および `#presentation-wrapper` で囲み、その中に各スライドセクションを配置します。

各スライド（`<section class="slide">`）は、以下の要素で構成するのが基本です。
- **ヘッダー (`.slide-header`)**: ロゴやカテゴリタグを配置します。
- **タイトルエリア (`.slide-title-area`)**: 大見出しやサブタイトルを記述します。
- **コンテンツエリア (`.slide-body`)**: カードレイアウトやテキスト、画像を配置します。
- **フッター (`.slide-footer`)**: 自社名やページ番号を配置します。

### 余白と縦方向の配置
- 通常スライドは、`padding: 45px 80px 35px` を基準に、ヘッダー・タイトル・本文・フッターの余白を確保します。情報を詰めるためにこの余白を縮めません。
- `.slide-body` は原則 `justify-content: center` とし、タイトル下にカード群が貼り付かないよう、利用可能な高さの中央に配置します。
- 表紙はタイトルを画面中央に配置します。表紙タイトルはサービス名・テーマだけを基本とし、「ご提案書」を機械的に付け足しません。補足が必要な場合はサブタイトルで伝えます。

---

## 3. よく使うレイアウトとコンポーネント

> **レイアウトパターン集**: ここに挙げるコンポーネントを含む再利用パターンの実装カタログを **[patterns.html](./patterns.html)** に用意しています（比較テーブル、ダーク面カード、Before/Afterフロー、As-Is/To-Be/Action、4象限マトリクス、ロードマップ、横型ステップフロー、タイムライン3×2、詳細3カラム＋インサイト、目次、中表紙、数値ハイライト、人物紹介、担当者・会社概要、ソース相関図、**小パネル一覧（4×3）**、**ポジション別ロードマップ（スイムレーン）**、**制作プロセスと役割（ウォーターフォール／アジャイル）**、**出典つき主張（証拠バッジ／結論帯／出典行）**、**仕組みの全体像（レーン×フェーズ）**）。各スライドのHTMLと対応CSSブロックをコピーして使い、新しいパターンを作ったら patterns.html に1スライド追加して正本化します。

- **出典つき主張（PATTERN 33）の3部品**: 数字や調査結果をもとに主張するページは、この3つをセットで置く。
  - **証拠バッジ（`.evidence-tag`）**: タイトルの上に置き、そのページの主張が「実測（既定・塗り）」「外部調査（`.tone-external`・グレー）」「解釈・仮説（`.tone-interp`・線のみ）」のどれかを先に宣言する。9章の「事実か仮説か」を章単位ではなく1枚単位で見せる仕組み。「たぶん」と書いたページに `解釈` が付いているだけで誠実に見える。
  - **結論帯（`.takeaway-band`）**: 図表の下に「だから何」を1行。読み手が図を読まなくても帯だけ拾えば結論が取れる。強調は `<strong>` 1か所まで。ダーク面では使わず、サブタイトルに結論を書く。
  - **出典行（`.slide-source`）**: フッター直上に細く1行。「出典：◯◯実測（日付・n=）」「出典：△△調査（年月・n=）」の形で、自分で測ったものと借りたものを分ける。**数字を載せたページには必ず置く**（8章「ソースがない情報は排除する」を、読み手に見える形で守る）。「ソースは？」と聞かれる前に答えておく。

- **小パネル一覧（`.panel-grid` / PATTERN 29）**:
  - **粒度の揃った項目を10〜12個、1枚で見渡させて選ばせる**ための型です。課題の棚卸し、選択肢の提示、テーマ候補の一覧などに使います。
  - **カード比較（`.grid-2` / `.grid-3`）との使い分けが重要です。** カード比較は「**3案を読み込ませて選ばせる**」型で、1案あたりの情報量が多いとき。小パネル一覧は「**12個を眺めて指させる**」型で、1項目が短く、数で全体像を見せたいとき。**選択肢が4つ以上あるならこちらを使います。**
  - 構成は `.panel-card` に `.panel-card-header`（`.panel-title` ＋ 右上の `.panel-badge`）／`.panel-desc`／`.panel-tags` の3段。最下部に `.panel-hint` で「この一覧をどう使うか」を1行添えます。
  - **バッジで色の役割を増やさないこと。** 分類は `.panel-badge` の既定・`tone-mid`・`tone-soft` の**トーン展開**で表現します（アクセント1色ルール）。出典の実装はカテゴリごとに別色を当てていますが、カタログ化にあたり1色へ寄せています。
  - `.panel-desc` は2行で自動クランプされるので、**1項目の説明は40字前後**に収めます。12個を超えるならページを分けます。強調したいカードにだけ `.emphasis` を付けます。

- **レイアウト（グリッド）**:
  - 2列レイアウト: `.grid-2`
  - 3列レイアウト: `.grid-3`
- **カード要素 (`.card`)**:
  - コンテンツを整理して見せるための基本単位です。
  - 同じ列に並ぶカードは、中のテキスト量に関わらず高さや余白（padding）を揃えて美しく見せます。
  - 基準は `border-radius: 12px; padding: 20px` とします。文字を小さくしたりカード内余白を削ったりする前に、文章を短くするか、スライドを分けます。
  - **ダークスライド用の配色トークン**: `template.html` は `.card` にライト用の背景色しか定義していません。`.slide-dark` の中でカードを使う場合は、白の半透明オーバーレイで代用するのではなく、`--c-surface-dark` / `--surface-border-dark` のようなダーク専用のカード背景色・枠線色を `:root` に用意し、`.slide-dark .card` に適用してください。実装例は `patterns.html` の PATTERN 02 にあります。
- **本文中の強調**:
  - 本文中の担当者名、ツール名、ステータスを、背景色付きのチップやピルで囲みません。本文は通常テキストで読み切れる状態を保ちます。
  - 強調が必要な場合は太字を最小限使い、チップを使う場合もカード冒頭の分類ラベルなど、本文外に限定します。
- **複数名のプロフィール**:
  - 2名以上を紹介する場合は、`patterns.html` の **PATTERN 13: 人物紹介（複数名）** を使います。`team-grid`、`team-card`、`team-avatar`、`team-name`、`team-role`、`team-bio` の構成を基本とします。
  - 2名の場合は `team-grid` を2カラムにします。写真・氏名・役割・短い紹介文を縦にそろえ、横長の経歴カードと混在させません。
  - 写真はスライドと同じフォルダへ置き、相対パスで参照します。役割名・肩書きはテンプレートから補完せず、案件で合意した表記をそのまま使います。
- **選考・運用フロー**:
  - 3ステップ程度の選考フローでは、ステップ見出しは `18px`、説明文は `14px` 以上を目安にします。カード内の余白を削って文字を小さくしません。
  - 面接や候補者対応を複数名で担う場合は、「誰が何を担うか」を具体的に書きます。例: 進行・条件確認、候補者連絡・日程調整、スキル評価など。体制や分担はヒアリングで確認できた内容だけを掲載します。
- **テーブル (`.slide-table`)**:
  - 数値や条件を行×列で比較して見せたいページ用のコンポーネントです。ヘッダー行を `--primary-dark` 背景・白文字にし、セルは白背景＋`--surface-border`の罫線で構成するのが標準パターンです。コピペできる実装は `patterns.html` の PATTERN 01 にあります。
- **タイムライン/ご利用の流れ (`.timeline-container`)**:
  - 6ステップ等の多ステップのプロセスを示す場合、1列で6個並べると極めて横幅が狭く見づらくなります。その場合は **3列×2段（3x2グリッド）のカードスタイル** にレイアウトし、カード間の `row-gap: 22px`, `column-gap: 20px` や内側パディング `18px 24px` などの十分な余白を設定してください。
  - カード内では、数字（`.timeline-num`）とタイトル（`.timeline-title`）を横一列（`display: inline-flex` や `vertical-align: middle`）に配置し、説明文（`.timeline-desc`）を左寄せで改行して配置すると、視認性と美しさが格段に向上します。
  - 矢印表示は `:after` で `→` を配置し、3つ目（右端）のカードでは非表示（`:nth-child(3n)::after { display: none; }`）にします。この実装は `patterns.html` の PATTERN 08 にあります。
  - **時期・日付は説明文に埋めず、独立した1行**として見せます（`13px` / `weight 900` / ライト面 `--blue-deep`・ダーク面 `--blue-soft`）。カード内は「番号＋タイトル」→「日付」→「説明文」の3段構成にします。PATTERN 08 は日付の置き場が無いので、この1行を足す前提で使ってください。時系列スライドの可読性は、日付が一覧で拾えるかどうかで決まります（日付を説明文の先頭に統合したところ、読み手から「前のほうが分かりやすい」と差し戻された実例があります）。
- **アニメーション (`.reveal`)**:
  - スライド表示時に要素をふわっと表示させるには、`.reveal` クラスを付与します（スクリプトによって順次 `revealed` クラスが追加され、フェードインが実行されます）。

---

## 4. スライド作成・編集時のチェックポイント
- **リロードしても最後に見ていたスライドに戻る**: `goToSlide()` で移動するたびに `history.replaceState()` でURLハッシュ（例: `#slide-7`）を更新し、ページ読み込み時にそのハッシュを見て該当スライドから再開するようにしています。編集→保存→リロードを繰り返すレビュー作業でも、毎回トップページに戻らず作業中のページから続けられます。
  - 仕組み上、ブラウザのアドレスバーのハッシュ部分に依存します。ハッシュを手動で消したり、ハッシュなしの別URLとして開いた場合は表紙（`slide-1`）から始まります。
- **スライド枚数を変更した場合の同期**:
  スライドを増やしたり減らしたりした場合は、以下の3箇所を忘れずに更新してください。
  - HTML内の各スライドセクションの ID（例: `slide-14`）
  - 下部ナビゲーションの目次（TOC）の `<option value="...">`
  - 各スライド右下の総ページ数表示（例: `1 / 15`）と、ナビゲーションの初期表示
  - 最終スライドの次へ進むボタンが無効化されること
- **画像のパス**:
  - スライドと同じフォルダ内に置いたWebPなどの画像は、相対パス（例: `./image.webp`）で参照します。
- **不要なリンク・ボタンの削除**:
  - 提案内容に直接関係のないCTAボタンやリンクは、スライド上での混乱を避けるため削除しておきます。
- **表記ゆれのチェック**:
  - 同じ概念を指す言葉（例:「コラム」と「解説コンテンツ」、社名の敬称、フッターの社名の表記ゆれ）が資料内で揺れていないか、`grep` 等で横断チェックする。特にスライドの並び替えや複製を伴う編集をした直後は、TOCラベルや旧セクション名が残っていないか確認する。
- **タイトルだけを通し読みする**:
  - 各スライドのタイトルと `.slide-subtitle` だけを上から順に読み、「背景→事実→解釈→提案→判断依頼」の流れが1本につながるか確認する（9章「標準ストーリー構成」参照）。1枚ずつ正しくても、通して読むと話が飛んでいることがある。
  - 前のページを見ないと意味が取れない代名詞（「これ」「その」「ここまで」等）で始まるタイトルは、具体的な主語に直す。

- **同時編集と文字化けの確認**（0章6・7も参照）:
  - 複数のAIツールで同じファイルを同時に編集しない。編集は1ツールずつ順番に行う。
  - AIツールでの編集直後は文字化け・タグの千切れが無いか確認する。`python3 -c "open('index.html', encoding='utf-8').read()"` がエラーなく通るかで機械的にもチェックできる。

### レビューで受けた指摘を、次の案件に効かせる

案件レビューで修正指摘を受けたら、その案件フォルダで直して終わりにしない。**「この案件だけの事情か」「他案件でも同じことが起きるか」を判定し、後者だけを このリポジトリ に戻す。**

- 戻し先の振り分けは `README.md` の「新しい知見を残すとき」に従う（ルール→ `STYLE_GUIDE.md`、再利用できる見せ方→ `patterns.html`、参照先の案内→ `README.md`）。
- 書くときは抽象論にしない。**次回そのまま判定できる1文**にする（「読みやすくする」ではなく「タイトルを代名詞で始めない」）。
- 既存のルールと競合する場合は、既存を優先する。変えるべきだと思ったときは、置き換えではなく先に相談する。

**Why:** 同じ指摘を別の案件で繰り返さないため。案件フォルダに閉じた修正は、次の資料に効かない。

---

## 5. ローカル確認方法
`file://` パスで直接HTMLを開くのではなく、簡易的なローカルサーバーを立ち上げてブラウザで確認することを推奨します。

1. コマンドラインでスライドのあるディレクトリへ移動します。
2. 以下のコマンドで簡易サーバーを起動します。
   ```bash
   python3 -m http.server 8787
   ```
3. ブラウザで以下のURLにアクセスします。
   `http://127.0.0.1:8787/[取引先名]/[プロジェクト名]/`

---

## 6. PDF書き出し手順
スライドは最終的に提出用にPDF化されます。HTML内の `@media print` メディアクエリは `1280x720`（16:9）の印刷レイアウト用に最適化されています。

- **`print-color-adjust` は全要素 (`*`) に指定する**: カードの背景色やテーブルのヘッダー色まで確実に印刷に反映させるため、`@media print` 内では `* { -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; }` のように全要素に適用します。`template.html` には既にこの指定が入っているので、コピーして作る際に消さないよう注意してください。
- **最終ページの `page-break-after` に注意**: `template.html` には `.slide:last-child { page-break-after: auto; }`（最終ページの後に余計な空白ページを作らないための例外）が入っています。他ファイルをコピーして作る際にこの一行を落とさないよう確認してください。

### Google Chrome からのPDF書き出し手順
1. ブラウザでローカル表示しているスライドを開きます。
2. キーボードの `Cmd + P`（またはメニューから「印刷」）を選択します。
3. 印刷ダイアログで以下の設定を指定します。
   - **送信先**: 「PDFに保存」
   - **用紙サイズ**: 「A4」または「レター」（自動的に `@page` 指定の `1280x720px` にフィットします）
   - **方向**: 「横」
   - **余白**: 「なし」
   - **オプション**: 「背景のグラフィック」にチェックを入れる（背景色やカードの枠線、画像を表示するために必須です）
4. 「保存」をクリックしてPDFを出力します。

### ⚠️ PDF書き出しはGoogle Chromeを使うこと（AIツール／エディタの機能で書き出さない）
AIツール／エディタ内蔵のPDF書き出し機能を使うと、**1枚目（表紙）が macOS プレビュー・Quick Look で真っ白になる**不具合が発生することがあります。

- **症状**: PDFの表紙だけが白紙。ただし poppler など一部ビューアでは正常に見えるため、環境によって「見える／見えない」が分かれ、原因の切り分けが難しい。
- **原因**: 印刷用CSS（`@media print`）の問題ではなく、**書き出しエンジンが表紙ページに macOS の描画エンジン（PDFKit）が解釈できない要素（半透明バッジや角丸の透過グループ合成など）を埋め込んでしまう**、書き出しツール側の互換性バグ。CSSをいくら修正しても再発します。
- **対処**: PDF化は必ず上記の **Google Chrome の「印刷 → PDFに保存」** で行う。バッチ処理したい場合は以下のヘッドレスコマンドでも可（表紙も正常に出力されます）。
  ```bash
  "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
    --headless --no-pdf-header-footer \
    --print-to-pdf="出力先.pdf" --virtual-time-budget=8000 \
    "file:///スライドのHTML絶対パス.html"
  ```
- **確認**: 書き出し後は必ず macOS のプレビュー／Quick Look で**1枚目（表紙）が表示されているか**を目視チェックしてから提出する。

### ⚠️ Chromeで書き出しても1枚目が白紙になるときは、印刷CSSの `body` を疑う

上の互換性バグとは別に、**印刷CSSの書き漏れで1枚目だけが紙の外へ出る**ことがある。こちらは書き出しツールの問題ではないので、Chrome の `--print-to-pdf` に変えても直らない。

- **症状**: ページ数は正しい（21枚なら21ページ）のに、1ページ目だけ本文が無く背景色だけになる。目次もフッターも出ない。
- **原因**: 画面用CSSで `body` をフレックスの中央寄せにしている構成（`display: flex; align-items: center; height: 100vh`）で、`@media print` が `height` と `overflow` だけを上書きし、**`display: flex` と固定高さを解除し忘れている**。印刷時は全スライドが縦に並ぶため、21枚分（15120px）の中身が高さ720pxの箱の中で上下中央に寄り、上へはみ出した分が紙の外に出る。1枚目が消えるのはそのため。
- **枚数が増えるほど出やすい。** 2〜3枚のうちは気づかず、スライドを足した日に突然1枚目が消える。**枚数を増やしたら書き出しを確認する。**
- **対処**: `@media print` で `body` の中央寄せを解除する。

  ```css
  @media print {
    html { width: 1280px; height: auto; overflow: visible; }
    body { width: 1280px; height: auto; overflow: visible; display: block; }
  }
  ```

- **`template.html` / `patterns.html` からコピーして作った場合は起きない。** どちらも `@media print` に `display: block !important` と `height: auto !important` が入っている。**この一行を落とさないこと**（6章冒頭の `print-color-adjust` や `page-break-after` と同じ注意）。テンプレを使わず印刷CSSを自作したファイルだけがこれを踏む。
- **確認はページ数ではなく中身で見る。** ページ数は正しく出るので、枚数一致は根拠にならない。

  ```bash
  pdftotext -f 1 -l 1 出力.pdf - | head
  ```

  何も返らなければ1枚目が空。画像で見たいときは `pdftoppm -f 1 -l 1 -png 出力.pdf out` を使う。

9枚のレポートスライドで実際に起きています。1枚目が丸ごと欠けたPDFが出ていました。

---

### 外部リンクは、PDFにしても押せる

用語集や出典から実物（Notion・Googleスライド等）へ飛ばしたいとき、`<a>` はそのまま使ってよい。**headless Chrome の `--print-to-pdf` でも、リンクは `/Subtype /Link` 注釈として保持される**（2026-08-28 実測）。書き出したPDFを配布しても押せる。

- **必ず `target="_blank" rel="noopener noreferrer"` を付ける。** スライドから離脱させないため
- スタイルは `.term-link`（`patterns.html` 収録）。アクセント色＋下線
- **リンク先の閲覧権限に注意。** 先方のNotionなど、開けない人がいる場合はその旨を小さく添える
- Notion外の資産（Excel等）にはリンクを張らず、**場所だけ書く**
- 用語集そのものの型は **PATTERN 30（用語集・外部リンク付き）** を使う

---

## 7. セキュリティと公開設定について
- **noindexの明記**:
  万が一の誤公開や一時的なサーバーアップロードに備え、スライドの `index.html` には必ず以下のメタタグを記述し、検索エンジンにインデックスされないようにします。
  ```html
  <meta name="robots" content="noindex,nofollow">
  <meta name="googlebot" content="noindex,nofollow">
  ```
- **個人情報の取り扱い**:
  スライド内に、先方担当者様の個人情報や機微な社内情報を直接掲載することは原則として避けてください。氏名・住所・顔写真・役職も同様です。
  **クライアントサイドのパスワードゲートは、静的ファイルへの直接URLアクセスを防げません。**（顔写真などの素材は、ゲートの裏側にあってもURLを直接叩けば取得できます。ゲートがあるから安全、とは考えないでください）
- **画像の出所**:
  ネットからの無断取得は禁止です。ご本人から提供いただいた素材、または自社制作の素材のみを使用します。
- **パスワードを可逆エンコードで埋め込まない**:
  base64 などはソースを見れば即座に解読されます。
- **個人情報を誤ってコミットした場合**:
  `git filter-repo --path <file> --invert-paths --force` で履歴を抹消 → remote を再登録 → force push、の手順が必要です。

---

## 8. コピーライティングの心構え
提案スライドの文言を編集する際は、以下のトーン＆マナーを意識してください。

- **誠実かつ客観的に表現する**:
  実績や効果を誇張せず、ヒアリングや商談で得た事実に基づいた客観的な提案を心がけます。主観的で過度に強い形容詞（「最高の」「究極の」など）は避け、相手のメリットを冷静に伝えます。
- **具体的で実用的な内容にする**:
  抽象的な表現や情緒的なフレーズ（ポエム調）は避け、「実務においてどのように機能するか」「どのような成果（アウトカム）が得られるか」を端的に記述します。
- **「新卒3年目が読んでわかるか」を基準にする**:
  読み手の想定は、**先方の新卒3年目相当**（業界の基本用語は通じるが、経営用語やフレームワーク名は通じない層）です。ここを基準に書くと、決裁権を持つ他部署の方にも通ります。専門用語や難解なカタカナ語は、**初出時に必ず一言で開く**こと。一文は短くし、必要に応じて箇条書きを活用します。
- **文体は常体（言い切り）にする**:
  見出し・リード文・本文とも「〜します」「〜です」を使わず、「〜する」「〜だ」または体言止めで書く。
  敬体はスライド上で冗長になり、1行に入る情報が減る。読み手は文章ではなく図として読むので、語尾が揃っているほど速く読める。
  - NG: `求人票の完成を待たずに、先に動かします` → OK: `求人票の完成を待たずに先に動かす`
  - NG: `記録率は33%。このままだと同じ結論しか出ません` → OK: `記録率は33%。このままでは同じ結論しか出ない`
  - NG: `枠を先に押さえないと、選考がそこで止まります` → OK: `枠を先に押さえないと選考がそこで止まる`
  - **例外**: 相手への依頼・確認（「ご確認ください」「ご記入をお願いします」）と、表紙・挨拶の文。ここは敬体のままでよい
  - **敬語そのものを消すわけではない。** 相手の動作には尊敬語を残す（`ご記入いただいた内容をもとに設計する`）。語尾だけを言い切りにする
- **読点は、消して意味が変わるときだけ打つ**:
  息継ぎのために打ちません。読点が増えるほど、本当に必要な読点が効かなくなります。判定は「その読点を消して意味が変わるか」だけで足ります。
  - **見出しには基本入れない**（一瞬で読む場所なので、一拍止まると逆にリズムが悪くなる）
    - NG: `基準と現在値を、同じ図に重ねる` → OK: `基準と現在値を同じ図に重ねる`
    - ただし逆接や結論の前置きは残す（`同じ基準でも、形は人によって違っていい`／`もう1本、別の軸がある`）
  - **1文に読点は2つまで。** 3つ目が要るなら、文を切る
  - **体言止めのあとは読点ではなく句点。** 読点だと並列に誤読される
    - NG: `価値観は「平和」、サーバントリーダーとして…`（「平和」との並列に見える）
    - OK: `価値観は「平和」。サーバントリーダーとして…`
- **比喩は、置き換えではなく補強に使う**:
  比喩だけで説明を終わらせません。**「事実を書いてから、必要なら比喩を足す」**の順にします。
  - NG: 比喩が主語になっている（「この組織は◯◯である」で説明が終わっている）
  - NG: 比喩の理解に別の前提がいる（相手の業界外のたとえ、社内ネタ、時事ネタ）
  - OK: 直前に書いた事実を、1行で言い換えるだけの比喩
- **商談メモやヒアリング内容に準拠する**:
  スライドを作成する際は、必ず事前に記録された商談メモ（`メモ.md` 等）に立ち返り、先方の抱えるリアルな課題や発言のニュアンスから外れないようにします。独自の解釈や推測で事実関係を膨らませすぎないよう注意してください。
- **ソースがない情報は排除する（AIが文章を勝手に作らない）**:
  スライドに載せる主張・基準・数値は、必ず議事録・商談メモ・ヒアリング等の一次ソースに根拠を持たせます。ソースを示せない情報は、もっともらしくても掲載しません（AIによる補完・創作をそのまま採用しない）。AIやスライド作成者の推論を入れたい場合はスライド本体には載せず、レビュー用メモに「要確認（推論）」として分離し、先方またはレビュー会議で裏が取れてから本体に反映します。
- **費用を投資として判断できる材料を示す**:
  費用や投資判断を含む提案では、作業内容と価格だけを示さず、可能な範囲で「現在の母数・改善対象の転換率・成果1件の売上または利益・費用回収に必要な追加件数・達成時期と前提条件」を整理して示します。目的は効果を約束することではなく、発注者がその金額を払う意味を判断できるようにすることです。未確認の数値は推測で補わず、「要確認」としてスライド本体から分離します。金額換算が不自然な提案には無理に適用しません。
- **相手企業のビジョン・価値観の反映（冒頭構成のルール化）**:
  スライド作成時には、必ず**スライドの冒頭部分（背景やご提案の意図など）に提案先企業の「ビジョン」や「価値観（バリュー）」を記述・反映**してください。先方の目指す方向性や企業理念と、自社のサポート・提案内容がどのように結びついているのかを明確にし、共創のストーリーを強固にするために必須のステップです。
- **AI時代に必要な4R（4Real）の観点**:
  AIで誰でもそれらしい提案書を作れる時代だからこそ、借り物ではない「生きた（Realな）」提案にするため、以下の4つのRを意識してコンテンツを構成・記述します。
  - **Review (レビュー／評価・主観)**: 単なる機能説明ではなく、他社や現状のやり方と比較して「なぜこれが優れているのか」を客観的かつプロとしての主観を交えて評価・提示する。
  - **Recipe (レシピ／活用提案・実践知)**: 抽象的な導入効果ではなく、「現場のスタッフが具体的にどう使い、どう価値に繋げるか」という実践的な手順やユースケース（レシピ）を示す。
  - **Record (レコード／事実の蓄積・記録)**: 「良くなります」という推測ではなく、過去の実績や商談時のヒアリング内容（`メモ.md` 等）などの確かな事実（レコード）を裏付けとして載せる。
  - **Relation (リレーション／信頼関係・つながり)**: 相手企業のビジョン・価値観と自社のサポートがどう結びつくか、顧客やメンバーとの信頼関係に基づく共創のストーリーを提示する。


---

## 9. 提案スライドの設計基準（提案型ベンチマーク）
提案スライドを作成する際は、自社で基準と決めた提案スライドのデザイン・構成を共通のベンチマーク（標準基準）とし、以下のガイドラインに沿って汎用的に設計してください。

### ① 配色と視覚的リズムの設計（Color & Theme Rhythm）
*   **ダーク＆ライトのハイブリッド構成**: 
    スライドは「ダークテーマ（`.slide-dark`）」と「ライトテーマ（`.slide-light`）」の2種類のテーマで構成します。
    *   **ダークテーマ**: 背景は `--grad-ink`（`#2c2c31` → `#0b0b0b` のグラデ。AM95のソール）、文字は白と `--text-muted`。アクセントは**文字色として**置きます。
    *   **ライトテーマ**: 背景は `#f2f4f6`、文字は `--c-ink` / `--text` / `--c-muted`（グレーランプの `#2c2c31` / `#424247` / `#63656e`）。真っ黒（`#000`）は使いません。
*   **アクセントの当て方（重要）**:
    ネオン系のアクセントは彩度が高く、白地に細字・細線で置くと消えます。**ライト面では「塗り＋濃いグレーの文字」、ダーク面では「文字色」** が原則。
    *   ライト面でどうしてもアクセントを線・記号として使う場合のみ `--accent-deep` を使う（✓マークなど装飾用途に限る）。
    *   アクセントは**単色**で使い、グラデーションにしません。グラデを使うのはダーク面の背景（`--grad-ink`）だけです。
    *   **どのトークンを使うかの早見表**（新規CSSを書くときは必ずこれに従う）:

        | 使う場所 | トークン |
        |---|---|
        | ライト面の塗り（チップ・バー・4px罫） | `--accent` |
        | その塗りの上に乗る文字 | `--on-accent` |
        | ライト面の線・✓などの記号 | `--accent-deep` |
        | **ダーク面の文字・バー・記号** | `--accent-on-dark` |
        | ごく薄いウォッシュ（面） | `rgba(var(--accent-rgb), 0.05〜0.12)` |
        | アクセントを薄く敷いた不透明な面 | `--accent-tint` |
*   **テーマ切り替えによるリズム感**:
    スライドを連続でめくる際のメリハリ（リズム）を重視します。ダークとライトを交互に配置するか、あるいは「現状分析・課題（ダーク）」から「理想・解決策（ライト）」へ移行するタイミングなど、論理展開の切れ目でテーマを大きく切り替えてください。

### ② タイポグラフィのヒエラルキー（Typography Scale）
*   **フォントファミリー**: 
    欧文・数字は `Inter`、和文は `Noto Sans JP` で統一します（1章の方針と同一）。`Outfit` は旧ベンチマークで使われていましたが、以降の最新ベンチマークでは不採用となったため使用しません。太字ウェイトの `Inter` でタイトルや強調数値の洗練さを表現してください。
*   **フォントサイズの厳格な制限（1280x720px基準）**:
    情報レイアウトが崩れないよう、以下のサイズスケールを遵守してください。値は `:root` の `--fs-*` 変数で持ち、個別に px を書かない。

    | 段 | 変数 | サイズ | 太さ・色 |
    |---|---|---|---|
    | 主役の数字 | `--fs-stat` | `48px` | 900・`--accent-deep`（PATTERN 12 `.stat-value`） |
    | 表紙タイトル | `--fs-title-lead` | `44px` | 900 |
    | 大見出し（スライドタイトル） | `--fs-title-slide` | `32px` | 900 |
    | サブタイトル | （`.slide-subtitle`） | `16px` | 600・`--text`（グレー） |
    | 中見出し（カードタイトル） | `--fs-card-title` | `18px` | 800 |
    | 本文 | `--fs-body` | `14px` | ライト600／ダーク700 |
    | 補足・キャプション | `--fs-caption` | `12px` | 400・`--c-muted`（読まなくていい顔をさせる） |

    **2026-09-18 に全段を1段小さくした**（旧: 50／36／20／15／13、サブタイトル18px・700・アクセント色）。
    **Why:** 読みやすい資料は本文が大きいのではなく、**上下の幅が広い**。主役の数字（48px）と補足（12px）で4倍の差があると、目が「数字→補足」の順に勝手に動く。旧スケールは15〜20pxの帯に本文・サブタイトル・カード見出しが集まり、どれを先に見るかが決まらなかった。サブタイトルを太字・アクセント色のままにするとタイトルと競り合うので、細く・グレーに落とした。
    **注意:** 本文を600の太さで3行以上並べると壁になる。太さを落とす前に行数を減らす（0章5「カードの本文は2行まで」）。`patterns.html` の各パターン内で個別に px 指定している箇所（表のセル・SVG内の文字など）は据え置き。触るときはその型ごとに見直す。
*   **本文テキストの太さはライト/ダークで変える**:
    本文（`card-text`, `card-list`, `insight-text`, `cjm-cell`, `step-desc`, `funnel-issue`, `mock-section-desc`, `flowchart-step-desc` など）は、背景テーマによって太さを変えることで可読性と印象のバランスを取ります。
    *   **ライトテーマの本文**: `font-weight: 600`。Regular（400）のままだと弱く頼りない印象になり、Bold（700）にすると小さいフォントサイズ（10〜15px）の日本語が潰れて逆に読みにくくなるため、中間の600が最適です。
    *   **ダークテーマの本文**: `font-weight: 700`（Bold）。暗い背景では600だとコントラストが弱く見えるため、ライトテーマより一段太くします。
    *   見出し（900）・カードタイトルやラベル（800）との太さの差は必ず残し、本文を800以上にはしないこと（階層が崩れます）。
    *   **注意**: `600`ウェイトは既定の Google Fonts の `<link>`（`Inter:wght@400;700;900`）には含まれていません。本文を600にする場合は `<link>` 側も `family=Inter:wght@400;600;700;900&family=Noto+Sans+JP:wght@400;600;700;900` のように `600` を追加すること（追加を忘れるとブラウザが最も近いウェイトへ自動フォールバックし、意図した太さになりません）。
*   **タイトルの配置位置**:
    *   **表紙（トップ）スライドのタイトル**: 画面中央（`text-align: center`, `align-items: center`）に配置し、ビジュアルインパクトを最大化します。
    *   **2ページ目以降（通常・個別）のスライドタイトル**: 画面左寄せ（`text-align: left`）で統一し、情報の視認性を高めます。

### ③ 標準ストーリー構成
構成は案件に必要な要素だけで組み、固定の枚数や結びスライドを必須にしません。知り合いの企業向けなどライトな提案では、表紙から必要な支援内容・体制・進め方へ短く入ります。

1. **表紙（ダーク）**: サービス名・テーマを端的に示す。
2. **目的・前提（ライト）**: 本提案のスコープや支援の意図を定義する。
3. **体制・役割（ライト）**: 誰が、どこまで担うかを明確にする。
4. **目標・方針（ダークまたはライト）**: 優先度と狙いを共有する。
5. **支援内容（ライト）**: 実務、選考、運用などを必要な単位で説明する。
6. **進め方・次のアクション（ライト）**: 定例、承認、開始準備を記載する。

クロージングは、意思決定や連絡先の明示が必要な場合だけ追加します。内容を補わない定型的な「結び」は入れません。

#### As-Is / To-Be / Action型（章立てのある提案向け）
現状分析や競合比較など情報量が多く、章として整理した方が伝わる提案では、以下の構成を代替パターンとして使います。中表紙（`.section-divider`、PATTERN 11）で章を区切り、ダーク⇄ライトの切り替えと合わせてリズムを作るのがポイントです。

1. **表紙（ダーク）**: サービス名・テーマを端的に示す。
2. **ソース相関図（ライト・任意）**: 調査量が多く、事実の出どころが複数ある提案では、サマリーの前に「**この資料が何からできているか**」を1枚置く（`patterns.html` の PATTERN 28）。中央に本資料、左右にソースを配置し、各ソースに「→ 本資料のどこに効いたか」を必ず添える。左右でグループの意味を分けると強い（例: 左＝先方からいただいたもの／右＝こちらで実測したもの）。**冒頭で出どころを開示することで、「外から持ち込んだ提案」ではなく「相手の中にあるものを整理して返している」ことが先に伝わる。** ソースが2〜3個しかない提案では省略してよい。
3. **サマリー（ライト）**: As-Is / To-Be / Action の結論を3カラムで先に一覧提示する（`.ato-container`、PATTERN準拠）。読み手が全体像を掴んでから詳細に入れるようにする。
4. **【章区切り】現状 (As-Is)**: 現状分析、競合ポジショニング（`.positioning-map`）、課題整理など、現状把握に関するスライドをまとめる。
5. **【章区切り】目指す姿 (To-Be)**: 解決の方向性、コンテンツ／施策戦略、顧客導線（カスタマージャーニー等）など、提案の中身をまとめる。
6. **【章区切り】アクション**: 今後の進め方・ロードマップなど、実行計画をまとめる。

- 各章区切りには`KEY MESSAGE`帯（`.flowchart-headline-main`、PATTERN 10のスタイル流用）で、その章に入る前に「この章で言いたいこと」を一文で示すと、カード羅列だけでは伝わりにくい論理が補強される。
- サマリー（2枚目）で結論を先出しした上で、後続の章区切りで同じテーマを掘り下げる構成になるため、サマリーと章区切りの文言が単純に重複しないよう、サマリー側は要点、章区切り側はその章の狙い、という役割分担を意識する。
- **使い分けの目安**: 支援内容がシンプルで枚数が少ない提案（10枚前後）は「提案型」、現状分析・競合比較・複数事業ラインなど情報量が多く章立てが必要な提案（15枚以上）は「As-Is/To-Be/Action型」を選ぶ。

#### As-Is / Issue / To-Be / Action型（仮説の提示・クライアントとの議論が前提の提案向け）
上記の As-Is/To-Be/Action型をベースに、TO-BEの前に「ISSUE（課題仮説）」を独立した章として挟む発展形です。各章の情報が「事実」か「仮説」かを作成者自身が意識し、書きぶりを変えるために使います。まだクライアントと方針合意ができておらず、たたき台を持ち込んで議論する商談・壁打ちフェーズの提案に向いています。

- **AS-IS（事実）**: ヒアリング・議事録・実績データなど裏付けのある事実のみを記載します。印象論で終わらせず、可能な限り正確な数値（件数・金額・期間等）まで踏み込みます。ソースを示せない数値は載せません（8章「ソースがない情報は排除する」参照）。
- **ISSUE（仮説・要議論）**: AS-ISから読み取れる課題を、提案側の主観的な仮説として提示します。ここで挙げるISSUEは、必ず後続のTO-BEの解決策につながるものに絞ります。TO-BEに接続しないISSUEは掲載しません。
- **TO-BE（仮説・要議論）**: ISSUEに対する解決の方向性を、同じく提案側の仮説として提示します。クライアントとの議論を前提とした「たたき台」であることが伝わる書きぶりにし、断定的な言い切りを避けます。
- **ACTION（仮説ベースの複数案）**: TO-BEを実現するための具体的な打ち手を、単一の決定事項としてではなく、複数の選択肢をパネル形式（`.grid-2` や `.grid-3` のカード比較）で並べて提示します。目的はクライアントの意思決定・視野拡大を促すことなので、提案側で1案に絞り込みすぎません。

**使い分けの目安**: 方針がまだ固まっておらず仮説をたたき台として持ち込みたい提案は「As-Is/Issue/To-Be/Action型」、すでに方針合意ができており自社の実行計画を明確に伝える段階の提案は、通常の「As-Is/To-Be/Action型」（単一プランのACTION）を選びます。

### ④ 情報のモジュール化とインフォグラフィック
*   **カードレイアウトの活用**:
    テキストをそのまま箇条書きにするのではなく、グリッドシステム（`.grid-2`, `.grid-3`）とカード（`.card`）を組み合わせ、情報を視覚的なブロック（モジュール）としてグループ化します。
*   **プロセスの視覚化**:
    タイムラインや段階的な変革プロセスなどは、矢印パーツなどを活用して論理の流れがひと目で直感的にわかるインフォグラフィックとして設計してください。
